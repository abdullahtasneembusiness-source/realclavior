NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — only used server-side (migrations, cron edge functions). Never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# Set once AI playbook generation (Phase 2b) and feedback distiller (Phase 2d) are wired up
ANTHROPIC_API_KEY=

# Set in Phase 6 (Stripe billing)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Set in Phase 6 (Resend transactional email)
RESEND_API_KEY=
